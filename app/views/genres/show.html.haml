.container
  -if user_signed_in? && current_user.try(:admin?)
    =render '/shared/admin_nav'
  %ul.list-unstyled#genre
    %li= image_tag @genre.image
    %li= @genre.title
    %li= link_to 'youtube', @genre.audio
    %li.text= @genre.description.html_safe
   

