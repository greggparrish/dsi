=render 'shared/header'
.container
  .row
    -@lessons.each do |l|
      .col-12.col-sm-6.col-md-3
        .col-12
          =link_to (image_tag l.image.sq_thumb), lesson_path(l)
        .col-12
          %ul.list-unstyled
            %li= l.title
  = paginate @lessons

