=render 'shared/header'
.container#page-content
  .row
    -@images.each do |i|
      .col-6.col-lg-3.img-grid
        = link_to image_tag(i.file.sm_sq), image_path(i), alt: i.title, title: i.title
  .clearfix
  = paginate @images
